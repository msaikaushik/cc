#include <iostream>
#include <string>
#include <fstream>

using namespace std;

int main(int argc, char *argv[]) {
	string arg = argv[1];
	string fileName = argv[2];
	cout<<arg<<":"<<fileName<<endl;
	//ifstream file (fileName);
	//if (file.is_open()) {
	
	//}
	return 0;
}
